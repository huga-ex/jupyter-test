name: Keep Streamlit Awake

on:
  schedule:
    # This runs the code every 6 hours
    # - cron: "0 */6 * * *"
    - cron: "0 * * * *"
  # This lets you run it manually to test it
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Visit Streamlit App
        run: |
          curl -sS https://jupyter-test-vzyser7a9gfvqeybeqhjgx.streamlit.app/
